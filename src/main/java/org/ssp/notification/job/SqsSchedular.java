package org.ssp.notification.job;
/*


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Configuration;
import org.springframework.scheduling.annotation.EnableScheduling;
import org.springframework.scheduling.annotation.Scheduled;
import org.ssp.notification.service.SqsReceiver;

import java.util.Date;

@Configuration
@EnableScheduling
public class SqsSchedular {

    @Autowired
    private SqsReceiver sqsReceiver;

    @Scheduled(fixedDelay = 2000, initialDelay = 1000)
    public void fetchData() {

        System.out.println("SQS Job Started  " + (new Date()));
        sqsReceiver.receiveFromSqs();
    }

}
*/
